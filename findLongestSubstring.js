// write a funciton called findLongestSubstring which accepts a string and returns the length of the longest distince substring

const findLongestSubstring = (str) => {
  let longest = 0;
  let charCount = {};
  let start = 0;

  for (let i = 0; i < str.length; i++) {
    let char = str[i];
    if (charCount[char]) {
    }
  }
};
